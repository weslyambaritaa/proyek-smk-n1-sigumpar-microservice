# ========================================================
# nginx.conf — Konfigurasi API Gateway
# ========================================================
# Peran Nginx di sini:
# 1. Reverse Proxy: meneruskan request ke service yang tepat
# 2. Load Balancer: bisa dikembangkan ke multiple instance
# 3. CORS Handler: header CORS ditangani terpusat
# 4. Rate Limiting: mencegah abuse (ada contohnya di bawah)
# ========================================================

# Jumlah worker process = jumlah CPU core
worker_processes auto;

events {
    # Maksimal koneksi simultan per worker
    worker_connections 1024;
}

http {
    # ── Logging ──────────────────────────────────────────────
    # Format log yang lebih informatif
    log_format main '$remote_addr - $remote_user [$time_local] '
                    '"$request" $status $body_bytes_sent '
                    '"$http_referer" "$http_user_agent" '
                    'rt=$request_time';

    access_log /var/log/nginx/access.log main;
    error_log  /var/log/nginx/error.log warn;

    # ── Performance ──────────────────────────────────────────
    sendfile on;           # Kirim file langsung dari OS, tanpa buffer user-space
    keepalive_timeout 65;  # Pertahankan koneksi TCP selama 65 detik

    # Kompresi response untuk menghemat bandwidth
    gzip on;
    gzip_types application/json text/plain;
    gzip_min_length 1000;

    # ── Rate Limiting ─────────────────────────────────────────
    # Batasi 10 request per detik per IP
    # burst=20 = izinkan lonjakan hingga 20 req sebelum ditolak
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;

    # ── Upstream Definitions ──────────────────────────────────
    # 'upstream' mendefinisikan server backend.
    # Nama 'users_service' dan 'todos_service' harus sama dengan
    # nama service di docker-compose.yml

    # upstream backend
    upstream backend_users_service {
        server users-service:3001;
    }

    upstream backend_todos_service {
        server todos-service:3002;
    }

    # upstream frontend
    upstream frontend {
        server frontend:5173;
    }

    # ── Virtual Host / Server Block ───────────────────────────
    server {
        listen 80;
        server_name localhost;

        # ── CORS Headers (berlaku untuk semua route) ──────────
        # Mengizinkan request dari frontend React
        add_header 'Access-Control-Allow-Origin'  '*' always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
        add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization' always;

        # Handle preflight request (OPTIONS) — browser mengirim ini sebelum POST/PUT
        if ($request_method = 'OPTIONS') {
            return 204;
        }

        # ── Health Check Nginx ────────────────────────────────
        location /health {
            return 200 '{"status":"OK","service":"api-gateway"}';
            add_header Content-Type application/json;
        }

        # ── Routing ke Users Service ──────────────────────────
        # Semua request ke /api/users/* akan diteruskan ke users-service
        location /api/users {
            # Terapkan rate limiting
            limit_req zone=api_limit burst=20 nodelay;

            # proxy_pass: teruskan request ke upstream 'backend_users_service'
            proxy_pass http://backend_users_service;

            # Rewrite URL: hilangkan prefix /api sebelum diteruskan
            # /api/users/123 → /users/123 di service
            rewrite ^/api(/.*)$ $1 break;

            # Headers yang diteruskan ke backend
            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Timeout settings
            proxy_connect_timeout 10s;
            proxy_read_timeout    30s;
        }

        # ── Routing ke Todos Service ──────────────────────────
        location /api/todos {
            limit_req zone=api_limit burst=20 nodelay;

            proxy_pass http://backend_todos_service;
            rewrite ^/api(/.*)$ $1 break;

            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            proxy_connect_timeout 10s;
            proxy_read_timeout    30s;
        }

        # ── Default: Route ke Frontend ───────────────────
        location / {
            proxy_pass http://frontend;
            proxy_set_header Host              $host;
            proxy_set_header X-Real-IP         $remote_addr;
            proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        }
    }
}